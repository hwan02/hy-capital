{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b 3.38.7 .flutter-sdk && .flutter-sdk/bin/flutter config --no-cli-animations && .flutter-sdk/bin/flutter pub get && .flutter-sdk/bin/flutter build web --release --dart-define=AUTO_EMAIL=$AUTO_EMAIL --dart-define=AUTO_PASSWORD=$AUTO_PASSWORD",
  "outputDirectory": "build/web",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
